import React from "react";

type Props = {};

const PlusIcon = (props: Props) => {
  return (
    <p className="text-[#878787] font-bold text-[24px] px-[11px] pb-[2px] rounded-[50%] hover:text-white hover:cursor-pointer hover:bg-[#3d3d3d] ">
      +
    </p>
  );
};

export default PlusIcon;
